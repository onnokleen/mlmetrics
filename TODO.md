# Book audit TODO (July 2026)

Complete line-numbered findings from the full-book audit (all 16 teaching chapters read in full; every exercise solution recomputed; empirical code executed where relevant; rendered HTML checked). Line numbers refer to the state of the files on 2026-07-02 and will drift as edits land — use the quoted anchors to relocate.

**Status 2026-08-07: Chapter 2 is fully closed.** The final six July-audit residuals were resolved: recursive/rolling pseudo-OOS terminology and citations, a discriminating leakage reflection question, a visually verified purge/embargo panel, the nested-CV procedure estimand, Exercise 2.1/2.2 notation cleanup, and the Exercise 2.3 Part 4 heading. New bibliography entries: `West1996` and `ClarkMcCracken2013`. `cross_validation.qmd` was rendered with `--cache-refresh`; all code cells executed and the output passed citation, cross-reference, and figure checks.

**Status 2026-08-06/07 (student-lens pass): Ch 2 and Ch 3 re-audited for pedagogy regressions** with the new project skill `mlbook-student-lens` (two independent runs, merged and deduplicated). Findings appended as a dated section at the **end of this file**; items already tracked below are cross-referenced there, not duplicated. Two current defects were introduced by July fixes (the Ch 2 Overview "nested validation" sentence from the :83 fix; the Ch 3 PIT index left on the origin clock by the :101 fix) — worth reading before working either chapter.

**Status 2026-07-28 (Tier 1 pass): 10 further items closed.** A triage pass over the 152 open items found that several were tagged `[S]`/`[T]` because they were *cheap to fix*, not because they were minor — they were in fact false or self-contradictory statements. Those were promoted and fixed: sigmoid range (global + Ch 7), Cramér distance (Ch 3), dropout (Ch 5), forget-gate weight-independence (Ch 7), loss-differential SE and scale-invariance (Ch 8), `mtry` attribution (Ch 11), Zhang–Yu attribution (Ch 12), Hyperband bracket counts (Ch 14). Three further open items were found **already fixed** by earlier passes and closed as stale (Ch 10 :111, Ch 15 :435 in part, `datasets.qmd` YAML title). New bib entry: `LiawWiener2002`. All seven touched chapters re-rendered; `?@sec-` check clean. **Caution for whoever works this list next: line numbers have drifted substantially — use the quoted anchors, not the numbers.**

**Status 2026-07-03: all 57 [E] items implemented.** Notes on the two non-obvious calls: (i) the conformal FRED-QD split cutoffs were changed in the *code* (quarter-end dates, so 1999Q4/2011Q4 are no longer orphaned) after verifying the new run (150/48/53 splits, q̂ = 6.771, coverage 50/53 ≈ 94%, high-VIX 9/11 ≈ 82%, width 13.5, MAE 3.16) leaves every prose-level number intact — the chapter needs a re-render; (ii) the Ch 5–7 no-Python item was resolved with explicit forward pointers to the chapter 8 pipeline, not new training code. New bib entries: BatesHastieTibshirani2024, ArlotCelisse2010, Vovk2012ConditionalValidity, LeiWasserman2014, FoygelBarberCandesRamdasTibshirani2021Limits, Barron1985, AlgoetCover1988. A new Exercise 7.2 (cell-state gradient highway) was added to the LSTM chapter; Exercise 15.3 got fresh numbers with a non-integer $(1-\alpha)(n+1)$.

Tags: **[E]** essential (false/misleading statement, broken output, or submission-level defect) · **[S]** style (rule-driven improvement) · **[T]** taste (judgement call). Lenses: **A** = methods rigor, **B** = writing precision, **C** = structure/CLAUDE.md conventions.

Verified clean (no action): bibliography (all 89 entries in `references-local.bib` cited, no dangling keys) · no `###` headings anywhere · all `number-offset` values correct · chapter skeletons complete (one exception: `nn_example.qmd`, see below).

---

## Global — production and publishing

- [x] **[E]** Draft chapters render publicly: `autoencoders.qmd`, `hidden.qmd`, `reinforcement_learning.qmd`, `decision_trees_full.qmd` are built into `docs/` and deployed (reachable via site search) because `_quarto.yml` renders `*.qmd`. Exclude them in the render list or move drafts out of the root.
- [x] **[E]** `adv_tree_based_methods.qmd:2` — only teaching chapter with a YAML `title:`; produces two `<h1>` elements in the built HTML (verified). Delete line 2. Check `datasets.qmd` (same YAML-title + `#`-heading pattern).
- [x] **[E]** `index.qmd:65,67` — `Coqueret and Guida @CoqueretGuida2020` / `James et al. @JamesWittenHastieTibshirani2013` render the author names twice; use bracketed citations.
- [x] **[E]** Broken list rendering (missing blank line before bullets; verified in built HTML): `information_theory.qmd:184-187`, `information_theory.qmd:247-250`, `evaluating_distributions.qmd:527-529` (also "notion" → "notions"/"notation").
- [x] **[S]** `#| echo: false` on figure chunks against the foldable-code convention: `information_theory.qmd:121,275,428`; `cross_validation.qmd:41,226,330`; `decision_trees.qmd:116,158,257`; `random_forests.qmd:130,230`; `gradient_boosting.qmd:125,360`. Switch to `echo: true` (global `code-fold` handles folding).
- [x] **[S]** Stale YAML comments "`# This makes sections 2.1, 2.2, etc.`" in `evaluating_distributions.qmd:3`, `feed_forward_nns.qmd:3`, `optimization.qmd:3`, `lstm_neural_networks.qmd:3` (and the draft files).
- [x] **[S]** Lecture-register leftovers: `feed_forward_nns.qmd:719` "in the lecture"; `recurrent_neural_networks.qmd:48` "For this course"; `lstm_neural_networks.qmd:289` "We do not cover GANs in class". A book should say "in this book/chapter".
- [x] **[T]** Repo hygiene: `deep-research-report.md`, `deep-research-report-5.md`, `notes.tex`, `test.tex`, `texput.log`, `rnn_diagram.log`, `_quarto_backup.yml`, `_quarto_clean.yml`, `_old_references.bib` in root — delete or move. *(Moved to gitignored `_archive/` 2026-07-28; not deleted. `notes.tex` is a draft of a different paper, Lönn–Kleen "Realized hedging opportunities" — relocate to that project rather than discarding.)*

- [x] **[E]** *(found 2026-07-28)* **Cross-file `@sec-` references do not resolve in this project and rendered as literal `?@sec-…` on the live site.** `_quarto.yml` is `project: type: website`, and Quarto only resolves crossrefs *within* a document for websites (books are different). Six were affected — `nn_example.qmd` → `@sec-hpo`, `random_forests.qmd` → `@sec-decision-trees`, `distributions_via_nns.qmd` → `@sec-eval-dist-empirical`, `adv_tree_based_methods.qmd` → `@sec-mini-log-score-kl` and `@sec-eval-dist-pit`, plus one introduced while fixing the Ch 2 `[Section 5]` item. All six converted to markdown links (`[text](file.qmd)` or `[text](file.qmd#sec-anchor)`), the pattern already used at `cross_validation.qmd:284`. **Same-file `@sec-` refs are fine and were left alone.** Note for future edits: do *not* convert cross-chapter links to `@sec-` refs in this project — that is what broke them.

## Global — cross-chapter notation harmonization

- [x] **[S]** Loss symbol: $L$ / $\ell$ / $\mathcal{L}$ / $l$ across chapters; in Ch 5, $L$ is simultaneously loss and layer count (both meanings in one formula at `feed_forward_nns.qmd:560`). Suggest book-wide: $\ell$ per observation, $L$ sample average. *(Resolved 2026-08-08 as a stable scoped convention per the methods check, not book-wide uniformity: Ch 4 uses $\ell_i$/$L$ with a notation map back to Ch 2's $L(y,\hat y)$/$\hat R$; the network chapters use $\mathcal{L}(\theta)$/$\ell$ with $L$ reserved for depth — declared once at Ch 5's Loss Functions section. Ex 5.3's likelihood made symbol-free (removing the $L(\theta)$-likelihood and $\ell(\theta)$-log-likelihood collisions); the regularization penalty renamed $P(\theta)$ so $R$ stays generalization risk. Ch 6/7 adopt $\mathcal{L}$ during their lens passes.)* *(Ch 6 converted 2026-08-10 — $\mathcal{L}_T$ terminal loss, target-first argument order; Ch 5 declaration widened to "this and the remaining network chapters". Ch 7 displays no loss symbols, so nothing to convert there; Ch 9's sum-vs-average $\mathcal{L}$ display is tracked in its chapter section.)*
- [x] **[E]** Ch 6 vs Ch 7 state contradictory Jacobians for the same object (`recurrent_neural_networks.qmd:233` transposed vs `lstm_neural_networks.qmd:141` correct). Fix Ch 6 (see chapter section).
- [ ] **[S]** Four weight-naming schemes across Ch 5–7: Ch 6 main text $W_{hh},W_{xh},W_{hy}$; ~~Ex 6.1 $W_h,W_x$; Ex 6.2 $W_{ih},W_{hh},W_{ho}$ (undefined in text)~~ *(Ch 6 side resolved 2026-08-10: Ex 6.1 now $W_{hh},W_{xh},b_h$; Ex 6.2 fully converted to $W_{xh},W_{hy}$ after its figure was recreated from a new TikZ source)*; Ch 7 recap $W_h,W_x$ still open. Also concatenation convention $W_f\cdot[h_{t-1},x_t]$ vs sum form never reconciled ($W_f=[W_{f,h}\;W_{f,x}]$ sentence fixes it).
- [ ] **[S]** Hidden state bold $\mathbf{h}_t$ (Ch 6) vs non-bold $h_t$ (Ch 7); input dimension $p$ (Ch 5) vs $D$ (Ch 6/7).
- [ ] **[S]** $\sigma$ collision in Ch 13: Gaussian scale and logistic function two bullets apart (`adv_tree_based_methods.qmd:214,265,267`); Ch 12 uses $\sigma$ = logistic only. Rename scale $s(x)$ or use $\Lambda(\cdot)$ for logistic.
- [ ] **[S]** $F$ overload: ~~forecast CDF vs true CDF (Ch 3, flips at `evaluating_distributions.qmd:225/486/508` and Ex 3.2)~~ *(Ch 3 part resolved 2026-08-07: $F$ = forecast, $F_0$ = truth)*; ensemble $F_m$ (Ch 12); Fisher info $F(\theta)$ vs CDF $\hat F(y|x)$ (Ch 13). Use $\mathcal{I}(\theta)$ for Fisher information (audience knows it from MLE).
- [ ] **[S]** Ch 11 is the only chapter using $\mathbb{V}$; every other chapter uses $\operatorname{Var}$.
- [ ] **[S]** Loss argument order flips between Ch 12 $\ell(y_i,F(x_i))$ and Ch 13 $\ell(\theta;y)$ — standardize or flag the switch.
- [x] **[S]** "Gate" overload: Ch 5 backprop calls $g'(z)$ the "local gate" (`feed_forward_nns.qmd:577-581,605`); Ch 7 defines gates as sigmoid units. Rename in Ch 5. *(Fixed 2026-08-08: "activation derivative" throughout — literal, vectorizes, generalizes to the activation Jacobian for softmax; "gate" now reserved for Ch 7.)*
- [x] **[S]** Sigmoid range: Ch 5:180 correctly $(0,1)$; Ch 7:133 claims $[0,1]$. *(Fixed 2026-07-28. Ch 7 was internally inconsistent — :133 said $[0,1]$ while :510/:550 said $(0,1)$. Rewrote :133 to state the interval is open because the sigmoid never attains its bounds, so $f_t=1$ is unreachable and the cell-state path always decays; the network controls the rate. Companion sentence added at the "gradient highway" display so $\prod f_k \approx 1$ is not read as exact preservation.)*
- [ ] **[S]** QRF naming drift in Ch 13: "Quantile Random Forest(s)" vs Meinshausen's "quantile regression forests" — pick one.
- [x] **[S]** Exam-annotation style: bare "*Exam level*" (`hpo.qmd:661,753,823`; `lstm_neural_networks.qmd:356`) vs descriptive annotations elsewhere — harmonize toward descriptive.
- [x] **[S]** Solution label format: "**Part k:** Title" (standard) vs "**Part k.**" (`foundation_models.qmd:651,720,802,913`) vs "Task k" (LSTM Ex 7.1) vs solutions split across three callouts (RNN Ex 6.1, `recurrent_neural_networks.qmd:310-391`). *(Statements in `foundation_models.qmd` still use "**Part k.**" — deliberate, only solutions were harmonized. Also removed the `---` rules between its exercises.)*
- [x] **[E]** Ch 5–7 contain **no Python implementation** (Ch 5 chunks only plot; Ch 6/7 only build TikZ figures) despite CLAUDE.md's "Theory + implementation balance." Add a minimal fit/forecast example to Ch 6/7 or explicit forward pointers to Ch 8.

## Global — missed econometric bridges (one-paragraph additions each)

- [x] Ch 2: expanding/rolling window = recursive/rolling pseudo-out-of-sample schemes (West 1996; Clark–McCracken) — never named. Also qualify "K-fold unsuitable for time series" with Bergmeir–Hyndman–Koo (2018). *(Closed 2026-08-07: recursive/rolling pseudo-OOS terminology added with new entries `West1996` and `ClarkMcCracken2013`; the Bergmeir qualification had already been added in the student-lens pass.)*
- [x] Ch 4: AdaGrad's accumulator $\sum g g^\top$ = OPG/BHHH information estimator; adaptive scaling = quasi-Newton/Fisher-scoring preconditioning (`optimization.qmd:180-189`). Overview promises the MLE connection; this is where it lives. *(Done 2026-08-08 as a carefully delimited analogy per the methods check — BHHH: observation scores at a common $\theta$, inverse as curvature; AdaGrad: iteration gradients at changing $\theta$, inverse square root — "geometric rather than an identity". Defines **preconditioning**, anchoring Ex 4.1's usage. New bib entry `BerndtHallHallHausman1974`.)*
- [x] Ch 5: MSE fitting of $f(x;\theta)$ = nonlinear least squares (never named); NN-as-sieve literature (White 1990; Chen 2007); cite ridge at the weight-decay bullet (:721). *(Done 2026-08-08: NLS sentence at the MSE display; sieve paragraph after the UAT callout with the approximation-vs-consistency distinction [@White1990; @Chen2007]; ridge-type citation [@HoerlKennard1970] at the weight-decay bullet with the biases-excluded and decoupled-weight-decay caveats.)*
- [x] Ch 6: hidden state = *filtered* state (Kalman recursion) / observation-driven models (Cox 1981), not the latent state itself (`recurrent_neural_networks.qmd:48,182`). *(Done 2026-08-10 in the Ch 6 lens pass — see the chapter section.)*
- [ ] Ch 9: the Gaussian NLL at :82 *is* the GARCH quasi-likelihood with the recursion replaced by $g_{\phi_\sigma}(\mathbf{x}_t)$; Bollerslev–Wooldridge QMLE remark.
- [ ] Ch 10: tree = regressogram with data-chosen bins (sets up Ch 11's kernel-weights analogy at `random_forests.qmd:123`).
- [ ] Ch 12: componentwise L2-boosting = shrunken forward-stagewise regression (Bühlmann–Yu 2003) — the "sequential OLS on residuals" analogy named in CLAUDE.md is absent from the book.
- [ ] Ch 12↔13: Bernoulli Newton leaf step (÷ $\sum p_i(1-p_i)$ = Bernoulli Fisher info) = one-parameter Fisher scoring = Ch 13's natural gradient. One cross-reference sentence in §13.6.
- [ ] Ch 13: NGBoost can minimize CRPS (already defined in Ch 3) — unmentioned.
- [ ] Ch 15: classical regression prediction interval as opening contrast; normalized score at :526 = GARCH standardized-residual logic; Christoffersen coverage tests for §15.6's empirical coverage check.
- [ ] Ch 16: cite Loughran–McDonald (2011), Gentzkow–Kelly–Taddy (2019), Baker–Bloom–Davis (2016) at :15/:454/:502 (add to `references-local.bib`).

---

## Ch 1 — information_theory.qmd

**Status 2026-08-07 (round-4 external audit on the revised Ch 1): 10 of 11 findings applied + 1 sibling fix.** Genuine catches: the pointwise $+\infty$ conditions for continuous cross-entropy and KL replaced by measure-theoretic statements (the KL sibling at the definition found on our side, both now point to the Cauchy-vs-Gaussian example); the Econometric Interpretation callout and Key Takeaway 4 no longer state the discrete empirical-KL mnemonic as the general fact (underbrace now "average log-likelihood"); Ex 1.4 gains the transformed-support remark ($g(x)=e^x$) and swaps the wrong "growth rates" example for logs/units with an explanatory contrast; MI-estimation claims cited (new bib entry `KraskovStogbauerGrassberger2004`), kNN black-box gloss added, bias claim softened to this-realization estimation error with permutation-calibration phrasing, cross-panel comparability claim softened to qualitative reading; the KL decomposition now displayed in both $\mathbb{H}$ and $h$ notation; NLL/MLE expansion order fixed (MLE now expands in the Roadmap; KL de-expanded in Ex 1.3/1.4; "feature screening"→"predictor screening"); MI figure introduced; Bernoulli caption matched to the plotted grid ("approaches zero"); Ex 1.1 P2 upgraded to the $\tfrac12\log c$ entropy-difference derivation with the units reconciliation (new hint referencing the transformation rule); Ex 1.5 Parts 2–3 merged. Only moderation: F11's Ex 1.5 fix by merging rather than a new unequal-variance part. Chapter re-rendered; checks clean (1 KL expansion remains, 13 selective hints for 20 sub-parts).

**Status 2026-08-07 (student-lens pass): chapter audited and all items below closed.** Beyond the tracked items, the lens found and fixed: ML jargon in the Overview/MI callouts ("regularization and representation learning", "feature selection" → penalized-estimation phrasing with a Ch 2 link); duplicate KL expansion at the definition site; continuous mutual information now defined (previously only the discrete sum, while fig-mi-example estimates continuous MI); i.i.d. and GARCH expanded per the no-whitelist rule; Ex 1.2's $D_{\text{KL}}\ge 0$ premise cross-referenced to Exercise 1.3; the "How to Interpret This Figure" callout converted to main-text prose; a Bernoulli-figure intro sentence; "KL Divergence"→"KL divergence" capitalization harmonized; and new Exercise 1.7 (conditional KL for the Gaussian AR(1)). Chapter re-rendered; checks clean.

- [x] **[E/A]** :336 — "share support … Then $D_{\text{KL}}(p_0\|p_\theta)<\infty$" — false (Cauchy vs Gaussian counterexample: common support, infinite KL). Fix: shared support only rules out the support-violation $+\infty$; add $\mathbb{E}_{p_0}|\log(p_0/p_\theta)|<\infty$.
- [x] **[E/A]** :266 — "symmetric only if $\sigma_0^2=\sigma_1^2$ and $\mu_0=\mu_1$" — false and contradicted by own Ex 1.5 (equal variances alone give symmetry).
- [x] **[E/A]** :715,724-726 — Ex 1.3 solution gap: $\mathbb{E}_p[q/p]=\sum_{\mathrm{supp}(p)}q\le 1$, not $=1$; correct chain $D\ge-\log(\sum_{\mathrm{supp}(p)}q)\ge 0$; equality needs Jensen equality AND $\sum_{\mathrm{supp}(p)}q=1$; ":726 unconstrained outside support" misleading (equality forces $q=0$ there).
- [x] **[E/A]** :406-408 — "learning about one variable can only reduce uncertainty" — only on average; $\mathbb{H}(Y|X{=}x)>\mathbb{H}(Y)$ possible (Cover–Thomas flag this). Also :406 understates the discrete iff for equality.
- [x] **[E/C]** :106-116 — verbatim duplicate Bernoulli-entropy callouts (Example + Definition) — delete one.
- [x] **[S/A]** :425-499 — fig-mi-example presents a k-NN MI *estimate* on n=100 as the population quantity; continuous MI never defined (chapter defines MI for discrete only); caption not self-contained. Consider ARCH example ($r_t,r_{t-1}$ uncorrelated, MI > 0) per "econometric data, always". *(Closed 2026-08-07 lens pass: caption + estimate-vs-population framing were already fixed by earlier passes; continuous-MI definition added after the discrete one. The optional ARCH panel was not built — reopen if wanted.)*
- [x] **[S/B]** :84 — "generalized variance" analogy: term of art for $\det(\Sigma)$; entropy is relabeling-invariant so ignores outcome values — contrast with variance instead. *(Fixed 2026-08-07: "variance-like summary of uncertainty"; the relabeling caveat was already in place.)*
- [x] **[S/A]** :334-361 — MLE–KL section iid-only; add forward link to conditional log score/KL for dependent data (AR/GARCH factorization). :343 continuous-case empirical-KL caveat; :354 White limit needs "under regularity conditions (unique KL minimizer)". *(Verified already resolved 2026-08-07 lens pass: the "What changes under dependence" paragraph, the empirical-KL mnemonic caveat, and the White regularity clause are all in the chapter. Stale, closed.)*
- [x] **[S/B]** :208 — $\text{NLL}(q,X)$ used but never defined; $\mathbb{H}(\theta)$ overloads $\mathbb{H}(X)$; $I(x)$ (self-info) vs $\mathbb{I}(X;Y)$ visually adjacent; Ex 1.5 switches to capital $P,Q$. *(Closed 2026-08-07: NLL had already been defined by an earlier pass; $\mathbb{H}(\theta)$ shorthand remark added to the Bernoulli callout; $I$-vs-$\mathbb{I}$ disambiguation note added at the self-information definition; Ex 1.5 recast in lowercase $p,q,q_A,q_B$.)*
- [x] **[S/C]** — 16 hints for 16 sub-parts, mechanical; Ex 1.1 P2 hint states the answer; cut roughly half. *(Fixed 2026-08-07: deleted 1.1 P2, 1.2 P3, 1.3 P3, 1.4 P3, 1.5 P2, 1.6 P1, 1.6 P3; 12 hints remain for 21 sub-parts incl. new Ex 1.7.)*
- [x] **[T/A]** Ex 1.4 — "invertible" needs continuously differentiable, non-vanishing derivative (also :257 main text). *(Fixed 2026-08-07: $g'(x)\neq 0$ added to the :178 transformation rule, the Ex 1.4 statement, and its solution (inverse function theorem named). C¹ was already stated in the exercise.)*
- [x] **[T/B]** :221-226 reflection uses KL before introduced; :214 "log-score difference" before log score defined; :77 exclamation + scare quotes; :363-387 commented-out image-augmentation block (would violate content philosophy if restored); `***…***` pseudo-headings :242,260,268; "the figure above" instead of `@fig-` refs. *(Closed 2026-08-07: reflection answer, Ex 1.3/1.4 solutions, and the dependence paragraph reworded via cross-entropy/expected log-likelihood — no more Ch-3 "log score" leakage; the :77 exclamation fixed. Pseudo-headings, commented-out block, and "figure above" were already gone — stale sub-items.)*
- [x] **Exercises**: 6, all other algebra verified correct. Ex 1.1 P2 below bar (hint gives answer); Ex 1.5 P2–3 light back-to-back. **No dependence/non-iid exercise in the chapter** — add conditional-KL or AR(1) likelihood-factorization exercise. *(Closed 2026-08-07: new Exercise 1.7 — Gaussian AR(1) likelihood factorization, conditional KL via the two-Gaussian formula, stationary averaging, and the marginal-matching white-noise model attaining $\bar D=-\tfrac12\log(1-\phi_0^2)>0$; algebra verified. Two selective hints.)*

## Ch 2 — cross_validation.qmd

- [x] **[E/A]** :117 — "The whole point of CV is to approximate $R(\hat f)$" — conflates conditional risk with the CV estimand (expected risk of the procedure at training size $n(K-1)/K$; Bates–Hastie–Tibshirani 2023). Add estimand paragraph after :212.
- [x] **[E/A]** :216 — "higher $K$ → less bias … lower $K$ → higher variance" — bias w.r.t. what unstated; variance direction reverses the usual claim and is setting-dependent (Arlot–Celisse 2010). State the training-size bias mechanism; drop or qualify variance sentence.
- [x] **[E/A]** :914-916 — Ex 2.3: solution uses $\mathbb{E}[\xi_t|\mathcal{F}_t]=0$ but the statement assumes only unconditional mean zero. Add "$\{\xi_t\}$ iid innovations" (or independence from $\{z_s\}_{s\le t-1}$).
- [x] **[E/A]** :281 — measurability definition requires the validation *outcome* to be $\mathcal{F}_t$-measurable — impossible. Restrict condition to predictors + training data; the outcome is the subsequently realized target.
- [x] **[E/B]** :12 — "Nested validation … is the econometric analogue of what this chapter formalizes" — inverted; the econometric analogue is pretest/specification-search bias; nested validation is the ML answer.
- [x] **[S/B]** :275-394 — name recursive/rolling pseudo-OOS schemes (see bridges); ~~:284 qualify blanket "K-fold not suitable for time series" (Bergmeir–Hyndman–Koo 2018)~~. *(Closed 2026-08-07: §2.7 now maps expanding/sliding windows to recursive/rolling pseudo-OOS evaluation and cites @West1996 and @ClarkMcCracken2013.)*
- [x] **[S/A]** :535 — fold std is not a valid SE (folds share training data; Bengio–Grandvalet 2004) — add one sentence; students will t-test fold scores otherwise. *(Closed 2026-08-07 by the student-lens fold-SD fix; the added sentence cites @BatesHastieTibshirani2024 — already in the bib — instead of a new Bengio–Grandvalet entry.)*
- [x] **[S/A]** :785-789 — Ex 2.2 P2 strict inequality needs non-degeneracy (solution :862 quietly adds it) — put in the statement. *(Fixed 2026-08-07, round-2 lens pass: $\Pr(\varepsilon_A\neq\varepsilon_B)>0$ added to the statement; solution now references the assumption.)*
- [x] **[S/C]** :185-188 — reflection question answered verbatim in the callout directly above — replace with a discriminating prompt (e.g., winsorizing at fixed vs full-sample percentile thresholds). *(Closed 2026-08-07: the replacement contrasts ex-ante $\pm5\%$ thresholds with full-sample 1st/99th percentiles and explains training-fold estimation.)*
- [x] **[S/C]** — hints mechanical (4+4+4); drop content-free ones (2.1 P4, 2.2 P2/P4, 2.3 P3/P4). *(Fixed 2026-08-07, round-2 lens pass: exactly those five deleted; 2.2 P1's min-identity kept as genuine scaffolding. 7 hints remain for 12 parts.)*
- [x] **[S/C]** ~~:41,226,330 echo:false~~; ~~:403-404 `nn-cv-tuning` label lacks `fig-` prefix~~; ~~:329 caption not self-contained~~ *(fixed 2026-08-07, lens pass)*; purged/embargoed CV (:300) deserves a third panel in @fig-timeseries-cv; ~~:398 hardcoded "[Section 5]" → `@sec-` ref~~. *(Closed 2026-08-07: the third panel distinguishes a pre-validation purge from a post-validation embargo and explicitly presents both as modifications of a forward split.)*
- [x] **[S/A]** :542 — nested CV estimates the performance of the tuning-plus-fitting *procedure* (selected HPs differ across outer folds) — one clarifying sentence. *(Closed 2026-08-07: the paragraph now names the complete procedure as the estimand and explains why selected configurations can vary across outer folds.)*
- [x] **[S/B]** *(added 2026-07-28)* §2.3 term-definition audit: the loss/empirical-risk/generalization-risk block sat **after** the figure that plots training and test MSE, and the same two quantities were named four ways ("training sum of squared errors", "Training MSE" in the legend, "training error" in prose). Fixed: block moved before the chunk; prose standardized on **training MSE**/**test MSE** with $\hat{R}_{\text{train}}$/$\hat{R}_{\text{test}}$; $\varepsilon_i\mid x_i \stackrel{\text{iid}}{\sim}\mathcal{N}$ stated so $f$ *is* the conditional mean; the independently simulated 200-observation test sample described in prose; $R(\hat f)=\mathbb{E}_{(X,Y)}[\,\cdot\mid\hat f]$ so §2.6's procedure-vs-fitted-model distinction has something to bite on. Also: $\hat\mu_d$ linked to $\hat f$, "testing set"→"test set", validation set defined at first use, "approximately $n(K-1)/K$", "High variance" randomness named, fold SD defined, DGP expanded.
- [x] **[T]** Notation: $\Delta_j$ ≡ $z_j^{\text{honest}}$ duplication (:606-608); $R$ overloaded (functional, scalar, estimates — the training/test empirical-risk collision is now fixed via subscripts, the functional-vs-scalar one is not); unremarked $n$→$T$ switch in Ex 2.1. *(Closed 2026-08-07: $\Delta_j$ removed, Exercise 2.1 consistently uses $n,i$, and Exercise 2.2 denotes the common scalar risk by $R_0$.)*
- [x] **Exercises**: 3, all algebra verified correct. 2.2 P1–2 light (scaffolding OK); 2.3 solution P4 heading mislabeled ("Why Random CV Becomes Infeasible" — the *forecast* is infeasible). *(Closed 2026-08-07: heading changed to "Why Random CV Evaluates an Infeasible Oracle.")*

## Ch 3 — evaluating_distributions.qmd

- [x] **[E/A]** :376 and :709 — "upward-sloping PIT histogram" description of skew mismatch contradicts the chapter's own figure (replicated: deficit near 0, hump left of center, only modest uptick at 1). Rewrite both.
- [x] **[E/A]** :698 — models ranked by raw average LogS/CRPS over 63 quarters, no DM/HAC test, LogS gap driven by 2020Q2 — while :112/:517 teach exactly this. Add DM-type comparison with HAC on the score-difference series, or a caveat noting the single-observation dominance.
- [x] **[E/A]** :135 — interval-gaming example inconsistent with the miss-indicator criterion at :133 (under it, always-infinite intervals dominate). Restate criterion as $|\text{coverage}-0.90|$ or change the gaming strategy.
- [x] **[E/A]** :369 — "marginal calibration (PIT uniform)" — clashes with GBR/Gneiting–Katzfuss terminology (that is *probabilistic* calibration; marginal calibration = average forecast CDF matches unconditional distribution). Rename; cite `GneitingKatzfuss2014` (already in bib, uncited); consider stating "maximize sharpness subject to calibration".
- [x] **[E/B]** :369 — $u_t=F_t(y_t)$ index clash (conditional on $\mathcal{F}_t$, $y_t$ known); use $F_t(y_{t+1})$ or shift index; add one-step-ahead clause to DGT iid-U(0,1) result.
- [x] **[E/A]** :335, :282 — strict-propriety classes unspecified (CRPS: finite first moment, $\mathcal{P}_1$; LogS: densities); kernel representation at :282 needs finite first moment. Cosmetic: $CRPS$ italic → $\text{CRPS}$.
- [x] **[E/C]** :527-529 — broken bullet rendering (verified in built HTML) + "notion" typo.
- [x] **[S/B]** :225 vs :486 vs :508 — $F$'s forecast/true role flips inside the CRPS material and again in Ex 3.2 — reserve $F$ for the forecast, or flag the callout-local convention and fix :508. *(Fixed 2026-08-07: $F$ = forecast everywhere; the truth is $F_0$ in the advanced callout and Ex 3.2; the generic PIT statement keeps its canonical form.)*
- [x] **[S/A]** :503 — $\int(F-G)^2dz$ is the **Cramér distance**, not Cramér–von Mises (which integrates against $dF$). *(Fixed 2026-07-28: renamed, plus an explicit clause distinguishing the two — Cramér integrates against Lebesgue $dz$, Cramér–von Mises against $dF(z)$.)*
- [x] **[S/A]** :678,:700 — 15 bins on 63 test quarters (~4.2 obs/bin), no uniformity bands, test-window size never stated in text; :449 promises serial-dependence PIT diagnostics never shown. State $n=63$, use ~8 bins, add bands or caveat. *(Fixed 2026-08-07: 8 bins, pointwise ±2-SE binomial band drawn and explained; new lead paragraph states $n=63$ and discharges the serial-dependence promise with an explicit caveat + pointer to new Exercise 3.4.)*
- [x] **[S/C]** :16-22 Roadmap omits the empirical application; :512-519 "Beyond LogS and CRPS" is a one-callout section — fold in; :3 stale YAML comment; :523 link FRED-QD description to `datasets.qmd`. *(Closed 2026-08-07: Roadmap item 6 added; Beyond callout folded into the end of §3.7 with a title; the datasets link was done in the lens pass; the :3 YAML comment was already removed by the July global fix — stale sub-item.)*
- [x] **[S/C]** :539,:653 — captions carry heavy interpretation (move to prose); :342 "Measurement Error Sensitivity: High/Low" table row asserted with zero supporting text — explain or cut. *(Row resolved 2026-08-07: @Kleen2024 cited with an explanatory sentence after the table. Captions trimmed to descriptive content 2026-08-07 — interpretation was already fully present in the surrounding prose.)*
- [x] **Exercises**: 3, all algebra verified correct. 3.1 P1 scaffolding, P4 below bar, hints mechanical (P1 restates, P4 gives answer); 3.1 P3 note criterion diverges at boundary (so FOC is the min); 3.2 a.e.-vs-everywhere equality clause + Tonelli parenthetical; **no dependence/forecast-comparison-testing exercise** — add HAC-on-score-differences or PIT-uniformity≠independence part. *(Closed 2026-08-07: 3.1 hints P1/P4 deleted, $\sigma_0^2>0$ assumed, global-minimum boundary argument added; 3.2 got Tonelli + right-continuity/a.e. clause; new Exercise 3.4 (HAC on score differentials: Bartlett-weighted variance, MA(1) long-run variance, naive-vs-correct ratio = 2 at $\theta=1$) with one hint and full solution.)*

## Ch 4 — optimization.qmd

- [x] **[E/A]** :163 vs :170-171 — momentum off-by-one: $g_t$ evaluated at $\theta^{(t-1)}$ but update maps $\theta^{(t)}\to\theta^{(t+1)}$ (gradient two iterates stale). Rewrite as $\theta^{(t)}=\theta^{(t-1)}-\eta v_t$ (AdaGrad/RMSprop/Adam already consistent).
- [x] **[E/A]** :971-990 — Ex 4.2 P2 solution is circular (cites the SRSWOR variance formula the part asks to show). Derive via inclusion probabilities $\pi_i=b/N$, $\pi_{ij}=b(b-1)/(N(N-1))$.
- [x] **[E/A]** :71, :85 — convergence claims with no conditions (batch GD "stable and direct"; Robbins–Monro "recovers convergence"). Add short formal box: convex + $L$-smooth + $\eta\le 1/L$ ⇒ $O(1/t)$; nonconvex smooth ⇒ $\|\nabla L\|\to 0$ only. GD diverges on the chapter's own Ex 4.1 quadratic if $\eta>2/a$.
- [x] **[E/A]** :231 — Adam bias-correction takes expectation over mini-batch randomness explicitly assumed away at :163 — one sentence reinstating the probability space.
- [x] **[E/A-B]** :690-695 vs :518,:523 — nonconvex comparison figure uses lr 0.01/0.001/0.3 across optimizers, undisclosed (first figure discloses its exception at :265). State the adaptive-scale rationale in text and caption.
- [x] **[S/B]** :120-123 — dependence reflection answer too vague; name the actual mechanisms: unbiasedness of the minibatch gradient survives dependence (linearity), variance changes, real dangers are stateful/sequence models and preprocessing leakage. *(Fixed 2026-08-07 lens pass: three-mechanism answer as specified, cross-referencing the new Ex 4.2 P4.)*
- [x] **[S/C]** :785-1111 — no econometric-diagnostic part in the exercise set. Cheapest fix: Ex 4.2 Part 4 on contiguous time-block vs random batches under serial correlation (formalizes the :115 reflection). *(Done 2026-08-07: Ex 4.2 P4 added — Bartlett-weighted block-gradient variance, contrast with the SRSWOR variance, econometric-validity tradeoff; hint + full solution; algebra verified.)*
- [x] **[S/A]** :168 — Hessian-PSD equivalence needs twice differentiability (ReLU next chapter is the non-smooth case); move convexity definition to a Definition callout near first use (:85 already needed it). *(Fixed 2026-08-07: Definition: Convex Function callout inserted before the SGD pros/cons where convexity is first used, with twice-differentiability; the inline definition in the Momentum paragraph removed.)*
- [x] **[S/C]** :125-150 — Epoch definition in `callout-tip` → `callout-note` per callout table; algorithm notation drift ($n_{e,b}$ duplicates $J$; $T$ not in Input; $L_j$ computed but unused). *(Fixed 2026-08-07: callout-note; $n_{e,b}$ replaced by $J$; $T$ and $n_e$ added to Input; $L_j$ marked "for monitoring".)*
- [x] **[S/B]** :24-56 — $\eta$, $\nabla$, loss used before `## Notation` defines them; :32 $l$ vs :35 $L$ vs $\ell_i$ — three loss symbols; move Notation up; rename the 2D demo function (:262, also axes labeled $\theta_1,\theta_2$ while defined in $(x,y)$). *(Fixed 2026-08-07: $l\to L$ at :32; demo renamed $L(\theta_1,\theta_2)$ and identified as Ex 4.1's quadratic with $a=0.2,b=2$; Notation left in place — $\eta$ is defined inline and the loss concept is glossed in the Overview, so the reorder proved unnecessary.)*
- [x] **[T/C]** :269,:523 captions not self-contained (panels/paths/markers undefined); :1003-1005 chapter-boundary callout sits between Ex 4.2 and 4.3 — move above `## Exercises`; :3 stale YAML comment. *(Fixed 2026-08-07: both captions carry path/marker/contour encodings; callout moved above ## Exercises; the YAML comment was already gone — stale sub-item. Bonus: post-figure interpretation paragraph added after the non-convex figure.)*
- [x] **Exercises**: 3, all verified except the circular 4.2 P2 solution. 4.1 P1 hint restates; 4.2 fixed-partition (main text) vs SRSWOR (exercise) mismatch worth a sentence; 4.3 exemplary (Schur–Cohn/AR(2) verified) but uses $\theta_t$ vs chapter's $\theta^{(t)}$. *(Closed 2026-08-07: 4.1 P1 hint deleted; partition-vs-SRSWOR bridging note added; Ex 4.3 recast in $\theta^{(t)}$ and $u_t$ — matching the chapter-wide momentum-velocity rename that resolves the $v_t$ velocity/second-moment overload.)*

## Ch 5 — feed_forward_nns.qmd

- [x] **[E/A]** :623 — invalid non-convexity inference (sign-varying cross-partial is compatible with PSD Hessians). Replace with a negative 2×2 principal minor at a specific point, or the sign-flip symmetry argument (two distinct minimizers whose midpoint has $w_2=0$).
- [x] **[E/A]** :76 vs :155-160 — layer-count convention contradiction: parameter-count formula treats $L$ as hidden layers + extra output layer, against the declared convention (layer $L$ *is* the output). Pick one.
- [x] **[E/A]** :91,:199,:560 — output-layer $\delta^{(L)}$ formula holds only for element-wise activations, yet softmax is recommended; give the softmax+CE result $\delta^{(L)}=\hat{\mathbf p}-\mathbf y$ (mirroring Ex 5.3's sigmoid case).
- [x] **[E/A]** :696 — standardization advice never says "fit the scaler on the training split only" — the canonical leakage vector; also missing from the Common Pitfalls box (:747 catches shuffling only).
- [x] **[S/A]** :637 — "$\mathbb{E}[Y|X=x]=f(x;\theta)$" asserts correct specification ("we model/approximate"); :36 single-index/GLM hierarchy inverted. *(Fixed 2026-08-07 lens pass: approximation stated with correct-specification clause; GLM nesting corrected — fixed link = GLM-type, unknown link = single-index generalization.)*
- [ ] **[S/B]** :549,:560,:621,:1095 — symbol overloads: $L$ loss vs layer count in one formula; $H_{jk}$ Hessian vs $H$ width; Ex 5.3 reuses $L(\theta)$ for the likelihood.
- [x] **[S/B]** :587-591 — callout conflates optimizer's parameter iterate with the network's data input; per-example-then-average gradient convention never stated. *(Fixed 2026-08-07: callout now starts at parameter value $\theta^{(t)}$, runs the passes per observation, and states the average-over-batch convention with a link to the optimization chapter.)*
- [x] **[S/A]** :723 — dropout: units dropped independently w.p. $1-p$ ($p$ = retention), not a fixed fraction; test-time rescaling unmentioned. *(Fixed 2026-07-28: independence + binomial count stated, test-time scaling by $p$ / inverted-dropout equivalence added, and the Srivastava-vs-Keras convention clash on $p$ flagged since the book uses Keras in Ch 8.)*
- [x] **[S/C]** :28-649 — "Basic Architecture" spans ~620 lines and swallows roadmap items 2–4; split into `##` sections so the TOC matches the roadmap. *(Done 2026-08-07: new sections Activation Functions and Universal Approximation, Loss Functions and Estimation, Backpropagation, Non-Convexity and Econometric Interpretation, with anchors; the four converted bold pseudo-headings removed; #sec-def-multilayer-nn kept on Basic Architecture.)*
- [x] **Exercises**: 3, all algebra verified correct (ReLU representation, gradients, $g'\le 1/4$ bound, $\partial\mathcal{L}/\partial z=p-y$). Ex 5.2 P2 wording implies the uniform bound explains saturation (solution prose handles it — align wording); hints mechanical (P1 hints of 5.2/5.3 restate setup). *(Closed 2026-08-07: 5.2 P2 reworded — bounds are caps, saturation means $g'\approx 0$; hints 5.2 P3, 5.3 P1, 5.3 P3 deleted (6 remain for 9 parts); 5.2 P1 kept for its chain-rule scaffolding.)*

## Ch 6 — recurrent_neural_networks.qmd

- [x] **[E/A]** :233 — Jacobian transposed: $\partial\mathbf h_i/\partial\mathbf h_{i-1}=\operatorname{diag}(g'(\mathbf z_i))\,\mathbf W_{hh}$, not $\mathbf W_{hh}^\top\operatorname{diag}(g')$ (that is the backward adjoint); product order unstated (descending $J_tJ_{t-1}\cdots J_{k+1}$). Contradicts Ch 7:141 (correct).
- [x] **[E/A]** :452-454 (premise :411) — **Ex 6.2 P4 model answer wrong**: $\partial h_2/\partial x_1=(W_{hh}\tanh'(z_2))(W_{ih}\tanh'(z_1))\approx 0.38$ — substantial memory; solution conflates near-zero state *value* with loss of dependence and contradicts its own $\le 0.8$/step bound. A student computing correctly would be marked wrong. Rewrite around geometric decay vs cancellation-by-new-inputs.
- [x] **[S/A]** — exploding gradients absent (Ch 7's table presupposes them); add paragraph after :242 incl. clipping. *(Done 2026-08-10: exploding-gradients paragraph with the scalar condition $|W_{hh}|\max_z g'(z)>1$ and gradient clipping, new bib entry `PascanuMikolovBengio2013`; section renamed "Vanishing and Exploding Gradients"; Overview, roadmap, and takeaways updated.)*
- [x] **[S/A]** — no window construction / truncated BPTT / batching of overlapping sequences / stationarity-as-justification for parameter sharing (:154) — the missing applied core for this book. *(Done 2026-08-10: new section "From One Series to Training Windows" {#sec-rnn-windows} — overlapping windows with per-origin training pairs and $\mathbf h_0=\mathbf 0$ re-initialization, windowed truncated BPTT with the truncation-horizon-returns-as-hyperparameter point and the state-carrying variant in one sentence, stationarity-justifies-parameter-sharing with the structural-break caveat; linked to Ch 2 chronological discipline, Ch 4 dependent-minibatch caveats, and Ch 8; Bridge-to-LSTM callout and implementation pointer moved to close the section; new pitfall on window length.)*
- [x] **[S/B]** :48,:182,:177 — analogue should be *filtered* state / observation-driven models (Cox 1981), not the latent state; "structural interpretation" of GARCH $\sigma_t^2$ misuses "structural". *(Done 2026-08-10: GARCH variance described as a filtered, observation-driven state citing new entry `Cox1981`; "structural interpretation" removed; the state-space callout now distinguishes exactly-computable observation-driven states from latent states requiring filtering.)*
- [x] **[S/C]** :310-391 — Ex 6.1 solutions split across three callout-tips; hint titles nonstandard; :293 Hint 1 hands over the formula P1 asks to derive. *(Done 2026-08-10: hints retitled "Hint for Part k"; Hint 1's give-away display removed; content-free Hint 3 deleted. The three-callout solutions claim was stale — already one block.)*
- [x] **[S/B]** :226 — output $\mathbf y_T$ vs target $y_T$ same letter, bold-only distinction. *(Done 2026-08-10: outputs renamed $\hat{\mathbf y}_t$ throughout, including both TikZ diagrams; terminal loss now $\mathcal{L}_T=\ell(y_T,\hat{\mathbf y}_T)$, target first as in Ch 5, with the $\mathcal{L}$-vs-$L$ convention restated.)*
- [x] **[S/B]** :134 vs :278 vs :400-404 — three weight-naming schemes in one chapter; $W_{ih}$, $W_{ho}$ never defined. *(Done 2026-08-10 for Ch 6: Ex 6.1 recast in scalar $W_{hh}, W_{xh}, b_h$ with an explicit scalar-case clause; Ex 6.2 initially kept $W_{ih}, W_{ho}$ for its hardcoded PNG; superseded same day by recreating the figure from a new TikZ source `figures/rnn_architecture.tex` with $W_{xh}, W_{hy}$ and $\hat y_t$ labels — the whole chapter now uses one scheme. Ch 7's recap notation remains open in the global item.)*
- [x] **[T/B]** :37 vs :53 near-verbatim duplication; fig-rnn-side-by-side caption doesn't explain node/arrow semantics; Ex 6.2's image (:396) has no caption/label/discussion. *(Done 2026-08-10: :53 rewritten as a section opener connecting back; caption explains circles/labels/loop/unrolling; Ex 6.2 image captioned and labeled `fig-rnn-exercise`, referenced from the statement.)*
- [x] **Exercises**: 2. 6.1 meets bar (spectral-norm extension correct); 6.2 P1–3 arithmetic verified, P4 wrong (above); no comparison-across-models part. *(Closed 2026-08-10: comparison part added as Ex 6.1 P4 — AR(1) impulse response $\phi^k$ vs gradient bound $|W_{hh}|^{T-t}$, DGP-property vs estimation-algorithm distinction; hint, solution, and annotation added; annotation no longer author-facing.)*

**Ch 6 round-2 external review applied 2026-08-10** (12 findings, all applied): the exploding-gradients claim corrected — per-factor Jacobian norms above one do *not* imply product growth (submultiplicativity), so $\lVert\mathbf W_{hh}\rVert>1$ is stated as necessary, not sufficient; the vanishing condition stated as a per-factor bound $c<1$ replacing "on average"; the Ex 6.1 spectral-radius claim restricted to the linear recurrence. The local-vs-total $\partial h_t/\partial W_{hh}$ convention (held-fixed $h_{t-1}$) now stated in the main BPTT decomposition and the Ex 6.1 solution. Ex 6.1 P4 recast around three objects — DGP impulse response, fitted-model state sensitivity, credit assignment — removing the false DGP-vs-algorithm dichotomy. "Fixed-window training" named and separated from state-carrying truncated BPTT (state horizon vs gradient horizon; new entry `WilliamsPeng1990`). Parameter-sharing paragraph retitled "imposes time-homogeneous dynamics" with stability as the plausibility condition and the inputs-need-not-be-stationary caveat. Generic step-index vs calendar-time clock declared at the interpretation sentence and the terminal loss. `figures/rnn_architecture.png` recreated from new TikZ source with $\hat y_t$/$W_{xh}$/$W_{hy}$; Ex 6.2 P1–3 merged into one forward-pass part with all required tanh values moved into the statement (pen-and-paper constraint) and the answer-revealing hint deleted. Roadmap glosses for parameter sharing/unrolled graph/truncation; ARMA, GARCH, AR(1), i.i.d. expanded chapter-first; Bridge callout's "rarely the best" replaced by a criterion-based statement.

**Ch 6 round-3 external review applied 2026-08-10** (3 findings, all applied): (1) the timing overload fully resolved — unhatted $y_s$ now means only the calendar-time outcome; the terminal and per-step losses are written in the forecasting convention $\mathcal{L}_T=\ell(y_{T+1},\hat{\mathbf y}_T)$, $\sum_t\ell(y_{t+1},\hat{\mathbf y}_t)$; scalar forecast objects use the book's two-index default $\hat y_{t+1\mid t}$ (setup display, $q_\theta$ display, Memory bullet, Outputs section), with generic outputs staying origin-indexed $\hat{\mathbf y}_t$ to match the diagrams (the reviewer's $r_t$ relabeling was unavailable — $r_t$ is the return in the GARCH section). (2) The $c$/$c$/$c$ symbol collision in the gradients section split into $\kappa<1$ (contraction), $\gamma>1$ (scalar expansion), $c$ (clipping only). (3) Ex 6.1 P4(ii) corrected: for an AR(1) the training signal about $\phi$ is local ($\mathbb E[y_{t+1}\mid\mathcal F_t]=\phi y_t$ — adjacent-step gradients don't vanish), so the part now asks why the bound does **not** bind there and for which structure it does (delayed-signal $y_{t+1}=\theta x_{t-k}+u_{t+1}$), with the credit-assignment lock-in kept for that case; "mean-zero" added to the innovation assumption; hint and annotation updated with the unit.

**Ch 6 round-4 external review applied 2026-08-10** (2 findings, both applied): Ex 6.1's overloaded Part 4 split into Part 4 (AR(1): impulse response, DGP-vs-fitted-model memory, why the bound does not bind locally) and Part 5 (delayed-signal DGP and credit-assignment lock-in). Both designs now fully specified in the prompts — the AR(1) part states the observation structure ($x_t = y_t$, one-step-ahead loss per origin) and Part 5 gives $y_{t+1}=\theta x_{t-k}+u_{t+1}$ with uninformative intermediate inputs explicitly, so the solution no longer supplies decisive conditions absent from the prompt. Hints split (new Hint for Part 5), solution headings and annotation renumbered with the unit.

## Ch 7 — lstm_neural_networks.qmd

- [x] **[E/A]** :216-220, :289 — Chen–Pelger–Zhu case study misdescribed (verified against the paper, Mgmt Sci 2024): (i) 46 characteristics + 178 macro series, not 94; (ii) 1967–2016, not 1957; (iii) objective is the no-arbitrage GMM moment condition $E[M_{t+1}R^e_{t+1,i}g(I_t,I_{t,i})]=0$ with adversarial test assets, **not** MSE — the paper argues prediction-objective ML can underperform; ":289 GAN adjusts the objective beyond MSE" inverts the message. Quoted Sharpe/R² numbers are correct. Either present MSE framing as Gu–Kelly–Xiu and CPZ as the no-arbitrage alternative, or rewrite Steps 1–3 around the moment condition.
- [x] **[E/C]** :328-356 — the chapter's only exercise is pure substitution into equations printed 40 lines earlier (disallowed by CLAUDE.md); no derivation/comparison/diagnostic anywhere. Add: derive $\partial C_T/\partial C_t|_{\text{gates fixed}}=\prod f_k$, compare to RNN bound $|W_h|^{T-t}$ (Ex 6.1), explain why the gates-fixed caveat matters.
- [x] **[S/A]** :135 — "gradient magnitude depends only on forget gates, not weights" — overclaim (true only for the gates-fixed cell-state path; forget gates are weight-dependent); the :169 Scope note corrects it — qualify in place. *(Fixed 2026-07-28. The bolded heading "Weight-Independent Gradients" was the worst part — that is the phrase students memorize — so it became "Gate-Controlled Rather Than Norm-Controlled", with the gates-fixed scope, the weight-dependence of $f_t$ itself, and a pointer to the Scope note all stated in the bullet. Also removes the inconsistency with the newly added Exercise 7.2, which is built on the gates-fixed caveat.)*
- [ ] **[S/A]** :28 — forget gate not in Hochreiter–Schmidhuber (1997); added by Gers–Schmidhuber–Cummins (2000) — one attribution clause.
- [ ] **[S/A]** :209 — "Linear Factor Models" label over a linear *characteristics* regression (Fama–MacBeth style) — label/equation mismatch.
- [x] **[S/B]** :133 — sigmoid range is $(0,1)$, so $f_t=1$ unattainable — gradients always attenuated, just controllably slowly (:162 narrative). *(Fixed 2026-07-28 together with the global sigmoid-range item; both :133 and the :162 narrative were addressed.)*
- [ ] **[S/C]** :178 — "$O(H^2)$ vs $O(4H^2)$" meaningless; say $4H(H+D)+4H$ ≈ 4× an RNN cell.
- [ ] **[S/C]** :228-240,:77,:252 — mermaid NN1 diagram has no label/fig-cap; one-line captions far from self-contained (TikZ gate symbols unexplained).
- [ ] **[T/C]** :3 stale YAML comment; :84-93,:259-267 `if (TRUE)` guards force TikZ rebuild every render (copy Ch 6's mtime pattern); :16 blog post as primary intuition pointer.
- [ ] **Exercises**: 1; all six values verified correct; "Task k" labels; the σ/tanh numerical values live in Hint 3 but are required data — move into the statement; bare annotation.
- [ ] **[T]** Chapter is the thinnest in substance (case study + figure code dominate); needs new material (BPTT-for-LSTM or estimation discussion) more than edits.

## Ch 8 — nn_example.qmd

- [x] **[E/A]** :463 vs :541 — tuning certifies configs by best epoch within 30 (implicit early stopping); final model trains 100 epochs on train+valid with no early stopping — selected HPs never evaluated under the deployed protocol. Record and reuse best epoch, or EarlyStopping in both stages; at minimum disclose.
- [x] **[E/A]** :549,:572 — $\exp(\hat z)$ is a conditional-median estimate of $RV$; MSE/QLIKE reward the conditional mean — log-target models handicapped without the $\exp(\sigma^2/2)$-type correction (identity stated at `distributions_via_nns.qmd:305`). Acknowledge or correct.
- [x] **[S/A]** :615 — per-period SD of squared-error differences conflated with the SE of the mean loss differential (HAC-adjusted, smaller by $\sqrt{n_{\text{test}}}\approx 15$–20); ":roughly 0.01" asserted without derivation. Fix both. *(Fixed 2026-07-28. $n_{\text{test}}=486$ read off the rendered run, so the factor is $\sqrt{486}\approx 22$. Paragraph rewritten around $\bar d$ vs $\widehat{\text{se}}(\bar d)=(\hat\omega^2/n_{\text{test}})^{1/2}$ with the HAC rationale; the undefended "roughly 0.01" threshold is deleted. The paragraph now says explicitly that the chapter does **not** establish which differences are statistically distinguishable, and separates that from the economic claim the 0.0003 gap does support.)*
- [ ] **[S/A-C]** :202-287 vs :488-491,:590-594 — dead level-target arrays (`X_lag_rv_*` etc.) never used; `tune_model` closes over log-target globals, so level-target rows aren't reproducible from shown code (text disclosure at :570 exists). Show an `eval: false` level-pipeline chunk or delete the arrays.
- [x] **[S/B]** :619 — "scale-equivariant" → scale-*invariant* (homogeneity of degree zero). (Adjacent underprediction-penalty claim is correct.) *(Fixed 2026-07-28; also tightened "common constant" to "common positive constant".)*
- [ ] **[S/B]** :572 vs :574 — $n$ vs $n_{\text{test}}$ in RMSE vs QLIKE in the same paragraph.
- [ ] **[S/C]** :4-6 — self-invented YAML fields (`chapter-type`, `exercises-required: false`) exempt the chapter from `## Exercises`; codify an empirical-illustration exception in CLAUDE.md or add 1–2 pen-and-paper exercises (QLIKE invariance/asymmetry; SE of a loss differential under dependence; HAR lookahead check).
- [ ] **[T/B]** :611 — caption garbled ("test-period realized log realized variance"); enumerate plotted series. :41 vs :574 forecast time-index convention shifts ($\hat z_{t+1|t}$ → $\widehat{RV}_t$) — one defining clause.
- [ ] Verified sound (keep): chronological split with origins formed pre-split; train-only scaling; HAR averages lookahead-free; fair HAR-OLS refit on train+valid (disclosed); shuffle=False; test block touched once; quoted 0.0003 LSTM–HAR gap matches table.

## Ch 9 — distributions_via_nns.qmd

- [x] **[E/A]** :232-235,:275,:292-295 — empirical application has no validation block; hyperparameters (32 units, lr 1e-3, 200 epochs) have no stated provenance; `X_trva` named "train+valid" but nothing validated — contradicts Ch 8's discipline on the same data/split. State a-priori fixing or carve out a validation block; also one clause on why scaling on 80% (vs Ch 8's 64%) is fine here.
- [ ] **[S/B]** :80-82 vs :435 — "up to constants": the displayed $\ell_t$ is 2×Gaussian NLL − $\log 2\pi$ (positive affine, not additive constant); inconsistent with Ex 9.1's correct ½ factors — harmonize.
- [ ] **[S/B]** :155 — "$K$ indexes the mixture components" — $k=1,\dots,K$ indexes; $K$ is the count.
- [ ] **[S/C]** :14-20 — Roadmap omits the empirical application; item 5 wrong about what closes the chapter.
- [ ] **[S/C]** :101 — hemisphere-network fig-cap not self-contained (boxes/core/heads undefined).
- [ ] **[S/B-C]** :331 — caption carries interpretation + "breathes" register + factor-1.6 claim (prose at :357 already covers it) — move to text.
- [ ] **[S/B]** :75-94 — add GARCH-QMLE bridge (see global bridges).
- [ ] **[T/A]** :61-65 — models condition on finite $\mathbf x_t$, not $\mathcal{F}_{t-1}$: the training sum is a partial/quasi-likelihood — one clause.
- [ ] **[T/A]** :401 — score difference read without noting it is untested (DM cross-ref clause).
- [ ] **[T/B]** — quantile-regression networks (pinball loss, crossing) absent from a "distributional NNs" chapter — one-sentence pointer preempts the question.
- [ ] **Exercises**: 2, all algebra verified correct (9.1 minimizer $e^2$, $\max\{e^2,c\}$ matches the chapter's own `VAR_FLOOR` code; 9.2 mixture variance and density limits). 9.1 P2–3 both 1–3 min — consider merging; hints mechanical (9.2 P2 hint *is* the answer; 9.1 P1 restates).

## Ch 10 — decision_trees.qmd

- [x] **[E/A]** :334-342 — cost-complexity objective has no optimization domain (subtrees $T\subseteq T_0$ by collapsing internal nodes) and no weakest-link/nested-sequence result (BFOS) — as written, ill-posed and infeasible. Two sentences + tie $\alpha$ tuning to the :346 warning.
- [x] **[E/A]** :111 — recursion has no stopping rule (never terminates as described). State base termination (pure node / min size / no improving split), then pre-pruning tightens it.
- [ ] **[S/A]** :304-317 — bias-variance display: $\varepsilon_0$ and bias target undefined; posit $Y_0=f(x_0)+\varepsilon_0$, independence, expectation over new draw + training sample.
- [ ] **[S/B]** :10 — "Regreesion" typo; monograph title is "Classification and Regression *Trees*".
- [x] **[S/A]** :111 — "guarantees a local optimum" — no topology in which that's true; say myopically optimal given prior splits. *(Already fixed by an earlier pass — verified 2026-07-28: :111 now reads "myopically optimal given the splits already made" and states the termination rule. Stale entry, closed.)*
- [ ] **[S/C]** :116,:158,:257 echo:false; :157 (and :115) captions not self-contained (fill/scatter colors, missing colorbar).
- [ ] **[T/B]** — regressogram bridge (see global); :149,:190 "The figure shows…" → `@fig-` refs; Ex 10.1 hints P1/P4 restate the task.
- [ ] **Exercises**: 2 (9 sub-parts), all arithmetic verified (stump SSE 5.1875; split SSEs 3.5/3.625/0.5; crossover α = 4.6875; entropies/Ginis; threshold 0.2). 10.2 P1–4 substitution-heavy, P4 a read-off — acceptable as scaffolding for P5, but consider tightening.

## Ch 11 — random_forests.qmd

- [x] **[E/A]** :111-121 — forest-weights identity stated as exact but inexact under bootstrap multiplicity (distinct-observation weights vs bootstrap-sample leaf means). Weight by multiplicity $m_{ib}$ or state the convention under which it is exact.
- [x] **[E/C]** :317 — "same as in constrained boosting" forward-references Ch 12 material; make self-contained here, dedupe the near-identical `monotonic_cst` block across chapters.
- [ ] **[S/A]** :43-69 — variance formula: state the probability space (randomization conditional on sample vs joint) and $\rho\ge 0$ for the lower-bound reading (formula valid for $\rho\ge -1/(B-1)$).
- [x] **[S/A]** :306 — $p/3$, $\sqrt p$ defaults misattributed to @Breiman2001RandomForests (his Forest-RI uses $m=1$, $\lfloor\log_2 p+1\rfloor$); cite Liaw–Wiener (2002) — new entry in `references-local.bib`. *(Fixed 2026-07-28. New bib entry `LiawWiener2002`; the sentence now credits the `randomForest` package for the defaults and states Breiman's own Forest-RI values. Citation verified to resolve in the rendered HTML.)*
- [ ] **[S/A]** :202 — "approximately honest" uses the Wager–Athey term informally; say "approximately out-of-sample" (the exercise solution at :715 already does).
- [ ] **[S/A-B]** :362 — conflates (i) iid resampling destroying dependence in training with (ii) OOB evaluation leakage (post-$t$ trees predicting date $t$); also mention block bootstrap/subsampling as a resampling-side remedy.
- [ ] **[S/C]** :517-533,:652 — Ex 11.1 P2 hint hands over the full derivation (whose target formula is printed in the body); 11.2 P2 is a 30-second multiplication. Trim hint to $\text{Cov}=\rho\sigma^2$; merge 11.2 P1–2; add one harder part (heterogeneous correlations, or finite-$B$ gap as MC error).
- [ ] **[S/C]** :130,:230 echo:false; fig-oob-convergence caption must say data are simulated iid (that's *why* OOB tracks test MSE there).
- [ ] **[T/B]** :39,:111 — $L$ subsample size vs $L_b(x)$ leaf collision (rename $\ell$ or $s$); subagging uncited; permutation-importance off-support pathology sentence (:375-383).
- [ ] **Exercises**: 2, all verified (1.872; 79.2%; $e^{-1}$; 184). Set below the difficulty bar overall; 11.2 P4 (real-time reasoning) is strong.
- [ ] Noted correct (keep): "forests cannot overfit" folklore handled properly at :296 ($B$ = Monte Carlo error; complexity in leaf size/depth).

## Ch 12 — gradient_boosting.qmd

- [x] **[E/A]** :267 — least-squares-to-pseudo-residuals ⟺ weighted classification holds only if the base learner outputs $\pm 1$ — stated for $y_i$, not for $h$; false for the regression trees used elsewhere. Add the condition; one clause that the AdaBoost correspondence covers the weighting, not the step size $\alpha_m$.
- [ ] **[S/C]** :723 — Ex 12.2 titled "…and Early Stopping" but no part touches early stopping; and the chapter's exercise set has no econometric-diagnostic part. Add Part 4: why choosing $M$ by random K-fold on an AR-dependent target biases validation loss downward vs a time-ordered split (ties to the :499-503 warning) — or retitle.
- [ ] **[S/B]** :58-104 — add the componentwise-L2-boosting/forward-stagewise paragraph (global bridges; CLAUDE.md's flagship analogy).
- [ ] **[S/C]** :125-126,:360-361 — echo:false + non-self-contained captions (define panels, red line, scatter, synthetic DGP; fig-gb-fredqd at :433 is the model to copy).
- [x] **[S/A]** :299 — "halving $\nu$ approximately doubles optimal $M$ [@ZhangYu2005]" — they prove consistency of early-stopped boosting, not this reciprocal; mark as empirical regularity (Friedman 2001) and move the citation. *(Fixed 2026-07-28: reciprocal relation now attributed to @Friedman2001Greedy as an empirical regularity, with @ZhangYu2005 retained for what it actually proves — consistency of early-stopped boosting, which is what licenses treating $M$ as the regularizer.)*
- [ ] **[T/B]** :238 — the contrast is probability scale vs *log-odds* scale (where $F$ lives), which is why the Newton step at :240 is needed — reword.
- [ ] **[T/B]** :524 — "flexible loss functions" strength claim vs only squared/Bernoulli/exponential derived; one sentence on quantile/Huber losses + subgradients at kinks, connecting to Ch 13.
- [ ] **Exercises**: 2, all arithmetic verified to the last digit (12.1 SSE chain; 12.2 $F_0=\log 3$, pseudo-residuals). 12.1 P1 is 2-min recall (scaffolding).

## Ch 13 — adv_tree_based_methods.qmd

- [x] **[E/A]** :97 — pooled-neighbor view claimed "equivalent" to the weight representation — exact only when the leaf containing $x$ has equal size in every tree (Ex 13.1 silently satisfies it: all leaves size 3). Say "approximately; exactly when…"; optionally new Ex 13.1 part deriving when the views coincide.
- [x] **[E/C]** :2 — YAML `title:` → duplicate `<h1>` in built HTML (verified). Delete.
- [x] **[E/A]** :257 — NGBoost update never displayed (no iteration index, no $\nu$, no $\theta^{(m)}(x)=\theta^{(m-1)}(x)-\nu\hat h_m(x)$, no early stopping). One display + carry-over sentence cross-referencing @sec-gb-regularization / @sec-gb-early-stopping.
- [ ] **[S/B]** :214,:265,:267 — $\sigma(x)$ = Gaussian scale and logistic within one list (see global notation).
- [ ] **[S/A]** :99-103 — Wager–Athey consistency callout omits the **iid sampling** assumption (the caveat this book most needs); add Meinshausen's own QRF consistency clause.
- [ ] **[S/C]** — only one figure in the chapter; add (i) NLL contours in $(\mu,\log\sigma)$ with ordinary vs natural gradient steps, (ii) step-function $\hat F(y|x)$ from Ex 13.1's pooled sample.
- [ ] **[S/C]** :338-366,:479 — hints: 13.1 P5 hint *is* the answer, P1 nearly; 13.2 P3 hint restates the definition. Delete.
- [ ] **[S/C]** :461-462 — 13.2 P3 (30–60 s) and P4 (1–2 min) below bar — merge; add reparameterization-vs-Fisher-scaling part ($\partial\ell/\partial\theta=\lambda y-1$ under $\theta=\log\lambda$ = natural gradient ÷ $\lambda$).
- [ ] **[T/B]** :61 — "estimates something like $\mathbb{E}[Y|X=x]$" — precisely the prohibited suggestive phrasing; state the estimator.
- [ ] **[T/C]** :28-40 — §13.2 re-derives weights from `random_forests.qmd:109` (@sec-rf-weights, which forward-links here) without a back-reference — open with "Recall from @sec-rf-weights…".
- [ ] **Exercises**: 2, all arithmetic verified (pooled CDF, median 12, P25 = 11; Poisson natural gradient 0.08).

## Ch 14 — hpo.qmd

- [ ] **[S/A]** :321,:331 — EI incumbent defined as the noisy min $c_{\min,t}=\min_j\hat c_j$ right after the chapter proves that min is optimistically biased; acknowledge and give the plug-in alternative $\min_j m_t(\lambda_j)$.
- [ ] **[S/C]** :732,:744-750 — Ex 14.2 P1 recall (1–2 min), P3 read-off (30 s); no derivation part in 14.2 or 14.3. Replace P1 with deriving the EI closed form from $\mathbb{E}[\max\{c_{\min}-C,0\}]$; give P3 content (TPE's EI increasing in $\ell/g$).
- [ ] **[S/C]** :664-686,:756-778,:826-848 — 12 hints for 12 parts; keep ~3 with scaffolding value.
- [ ] **[S/C]** :661,:753,:823 — bare "*Exam level*" → descriptive annotations (match foundation_models style).
- [ ] **[S/A]** :49-63 — prose claims optimism vs the *selected* configuration but the display proves it vs $\min_j c_j$; add the $\mathbb{E}[c_{\hat\jmath}]\ge\min_j c_j$ sentence.
- [ ] **[S/B]** :513-519 — $B$ uninterpreted (per-bracket budget in Li et al.; total ≈ $(s_{\max}+1)B$).
- [x] **[T/A]** :552-556 — schematic bracket counts [27,9,3,1]/[9,3,1]/[3,1] don't satisfy the $n_s$ formula displayed above (true values 27,12,6,4) and budgets are unequal — reconcile or flag the simplification in text. *(Fixed 2026-07-28. Confirmed the arithmetic: $R=27,\eta=3 \Rightarrow s_{\max}=3$, $B=108$, $n_s=\lceil 4\cdot 3^s/(s{+}1)\rceil = 27,12,6,4$. Figure now uses the correct starting counts (12,4,1 and 6,2), brackets are labelled by $s$, and per-bracket totals come out 108/99/108 — the near-equal-budget property that is the whole point of the construction. Caption states $R$, $\eta$, $B$ and notes the omitted $s=0$ bracket; prose invites the reader to check the counts against the formula. Figure re-rendered and inspected: the longer labels needed a wider left margin, so `figsize`/`xlim` were adjusted.)*
- [ ] **[T/B]** :428,:436 — $y$ and $\gamma$ each defined twice (unmerged edit); delete the second clause.
- [ ] **[T/A]** :656 — Ex 14.1 needs non-degenerate noise for the strict inequality (used at :701).
- [ ] **[T]** :77,:355 — `import math` lives in the wrong chunk (session-shared; breaks standalone).
- [ ] **Exercises**: 3; all solutions verified numerically (EI 0.0300 vs 0.0307; SH 324 vs 2187 = 6.75×; min-identity + induction). 14.1 exemplary (ideal derivation/comparison/diagnostic mix); 14.2/14.3 below bar (above).

## Ch 15 — conformal_prediction.qmd

- [x] **[E/A]** :1022 — jackknife+ "finite-sample marginal coverage result" — level never stated: Barber et al. (2021) prove ≥ **1−2α** (CV+ ≥ 1−2α−min(…)), requiring a *symmetric* fitting algorithm; interval targets 1−α so students infer the wrong guarantee. State both.
- [x] **[E/A]** :649-651,:754,:762 — prose says calibration spans 2000Q1–2011Q4; FRED-QD dates quarters by final month, so the code's `<= 10-01` cutoffs end at Q3 and **1999Q4/2011Q4 fall into no subsample** (verified by execution). Fix cutoffs to `12-01` or correct the prose (incl. fig caption :762).
- [x] **[E/A]** :754 — overcoverage explained by finite-sample discreteness — wrong: with $n_{\text{cal}}=47$, discreteness caps expected coverage at 44/48 ≈ 91.7% < observed 94.3%. Correct explanation: test-window sampling noise ($n=53$, binomial SE ≈ 4pp) + calibration-draw variability (Beta$(k,n{+}1{-}k)$) + non-exchangeability.
- [x] **[E/A-B]** :601 — fig-cqr-recalibration caption: "widening is largest where the band is already wide" — false; the conformal correction adds constant $2\hat q$ everywhere; adaptivity comes from the initial band. Rewrite caption.
- [x] **[E/A]** :416-423,:465 — chapter proves only the heteroskedasticity example; add the general distribution-free conditional-coverage impossibility remark + citations (Vovk 2012; Lei–Wasserman 2014; Foygel Barber–Candès–Ramdas–Tibshirani 2021) — new bib entries. Key Takeaway 5 otherwise invites "better score restores conditional validity".
- [x] **[E/C]** :1419-1444 — Ex 15.3 P1 numerically identical to the in-text "Jackknife+ by Hand" worked example (:921-977). Change numbers; make $(1-\alpha)(n+1)$ non-integer to exercise the ceiling logic. Also P3/P4 near-identical answers.
- [ ] **[S/A]** :1057 — ACI described only verbally; add $\alpha_{t+1}=\alpha_t+\gamma(\alpha-\text{err}_t)$ and its *deterministic* long-run guarantee ($|T^{-1}\sum\text{err}_t-\alpha|=O(1/(\gamma T))$, no distributional assumption — the flagship result for the book's theme); one sentence on weighted/nex conformal (Barber et al. 2023).
- [ ] **[S/A]** :98 — "inductive" introduced without its contrast; add a short full-(transductive)-conformal paragraph (refit on augmented data per candidate $y$; cost) — completes the efficiency ladder for §15.10.
- [ ] **[S/A]** :435,:449-450,:463 — "continuous CDF ⇒ strictly increasing on its support" false (disconnected support); symmetry assumed but never used. Assume everywhere-positive density; drop or use symmetry. *(Partially resolved — verified 2026-07-28: the strictly-increasing claim now correctly conditions on a strictly positive density on $\mathbb{R}$. **Remaining: the unused symmetry assumption.**)*
- [ ] **[S/B]** :554-593 — three hatted $q$'s collide ($\hat q_{\text{low}},\hat q_{\text{high}}$ functions vs conformal scalar $\hat q$); rename $\hat q_{\text{cal}}$; state which levels the initial quantile models target (α/2, 1−α/2) and that validity holds for any fixed band while efficiency depends on the choice.
- [ ] **[S/B]** :12,:526,:756 — econometric anchors (see global bridges: prediction interval / GARCH / Christoffersen).
- [ ] **[S/A]** :756 — high-VIX subgroup is 9/11 = 81.8%, binomial SE ≈ 12pp; "exactly the lesson from the theory" overclaims — report $n=11$ in prose, soften to "consistent with".
- [ ] **[S/B]** :272 — exchangeability "slightly weaker" than iid → *strictly* weaker + one exchangeable-but-dependent example (equicorrelated Gaussians / de Finetti).
- [ ] **[S/A]** :393 — "iid infinitesimal perturbation" undefined; use $U_i\sim\text{Unif}(0,\delta)$ with $\delta$ below the minimal score gap, or auxiliary-uniform rank tie-breaking.
- [ ] **[T/C]** :22-29 — Roadmap omits the real-data section; ordering of item 5 misleads. Future-split seam: §15.8 (time series) is the natural place if ACI content grows.
- [ ] **Exercises**: 4; all arithmetic verified (15.1 $k=18$, q̂=1.8, exact 18/20; 15.2 [28,52]; 15.4 covariances). 15.2 P4 reproduces the :568-593 derivation verbatim (recall); 15.4 best exercise ("such as ρ=0" → "iff ρ=0"). Missing exercise content: coverage-bound algebra, ties, jackknife+ level, CQR-vs-symmetric width.

## Ch 16 — foundation_models.qmd

- [x] **[E/A]** :61 (and :53) — "under stationarity the RHS does not depend on $t$" — false in general (conditioning history grows; finite-context transformer is neither fixed-order Markov nor a stationary function of the infinite past). Define $H(p^\star,p_\theta)$ as a Cesàro rate, state the condition on $p_\theta$ or flag as heuristic; a.s. convergence needs generalized SMB (Algoet–Cover 1988; Barron 1985), not Cover–Thomas Ch. 4.
- [x] **[E/B]** :406 — "$d_4$ … published before $t$ but cites material produced after $t$" — temporally impossible; intended point is retriever-side contamination (figure code marks $d_4$ admissible). Rewrite.
- [x] **[E/A]** :659,:666 — Ex 16.1: sum is $-17.6098$ (not $-17.6096$); PPL $=\exp(2.2012)=9.036$ (not 9.038). Part 3's numbers correct.
- [ ] **[S/C]** :74,:202,:278,:405 — four of six figures (`fig-fm-nextoken`, `fig-fm-attention`, `fig-fm-attenuation`, `fig-fm-timeline`) never referenced in prose — add one interpreting `@fig-` sentence each.
- [ ] **[S/C]** :634 — Ex 16.1 P1–2 not pen-and-paper answerable (no $\ln$ values supplied, unlike 14.2's Φ/φ table); supply values or ask for exact expressions.
- [ ] **[S/C]** :651,:720,:802,:913 — "**Part k.**" → "**Part k: Title**"; also `---` rules between exercises (:675,:769,:841) inconsistent with hpo.
- [ ] **[S/A]** :454,:502,:610 — dictionary baseline required three times, canonical anchors uncited (Loughran–McDonald; Gentzkow–Kelly–Taddy; Baker–Bloom–Davis) — add to bib + cite.
- [ ] **[S/B]** :510 — collinearity + ΔR² = 0.08 + "high R² ⟹ overfitting noise" internally inconsistent — rewrite the statistical explanation around overfitting noise in $z_t$ (large $k_z$/short sample).
- [ ] **[S/B]** :369 vs :448 — $x_t^{\text{text}}=G(D_t)$ then $z_t=G(D_t)$ — same object, two names; $H$ (cross-entropy vs persona distribution) and $h$ (horizon vs persona weight) overloads — unify/rename or flag explicitly (hpo :521 shows the pattern).
- [ ] **[T/B]** :387 — "all documented empirical examples run in this direction" — unsupported universal; soften or cite. :259 — "unordered discrete label" misfires (hawkish/neutral/dovish is ordered).
- [ ] **Exercises**: 4; 16.2 (EIV incl. non-classical cross-term $\rho\sigma_s\sigma_u(\sigma_u^2-\sigma_s^2)$) and 16.4 (bias decomposition; 0.7·3.3+0.3·5.3=3.9) verified exact — both exemplary; 16.1 P1–2 weak scaffolding redeemed by P3.

## Ch 17 — datasets.qmd + front matter

- [x] **[S/C]** `datasets.qmd:2,7` — YAML `title:` + `#` heading: check for the same duplicate-h1 rendering as adv_tree_based_methods (fixed there by deleting the YAML title). *(Already fixed — verified 2026-07-28: `datasets.qmd` has no YAML `title:`. Stale entry, closed.)*
- [ ] **[T/B]** `datasets.qmd:127,134` — "Realized variance, sum of squared 5-minute log returns in percent" — unit ambiguity (%² vs %); one clarifying clause.
- [x] **[E/B]** `index.qmd:65,67` — citation syntax (listed under Global; kept here for chapter-sweep completeness).

---

## Student-lens pass (2026-08-06) — Ch 2 & Ch 3

Merged, deduplicated union of two independent `mlbook-student-lens` runs. Reader model: econometrics prerequisites + chapters before the audited one, **zero ML knowledge**. Severity→tag mapping: blocking→[E], friction→[S], polish→[T]. Line numbers refer to the **2026-08-06** file state (post-drift from the July numbers above). Rule tags `[B-*]` are defined in `.claude/skills/mlbook-writing/principles.md`.

**Status 2026-08-07: all 12 [E] items fixed; both chapters re-rendered** (code cells untouched, notebook cache hit; `?@sec-` check clean; @Kleen2024 resolves in the built HTML). Notes on non-obvious calls: the §2.8 NN example was kept but reframed as a black-box tuning problem — the `make_regression` data swap remains open as its own [S] item; the Ch 2 leakage callout moved together with its reflection question into §6 after the K-fold definition; the single LogS/CRPS expansion now lives in the Ch 3 Roadmap with :140 switched to short forms; $\mathbb H_{\text{CRPS}}$ was renamed to $h_{\text{CRPS}}$ along with $h_{ce}$/$h$ for one coherent continuous-case convention. The fold-SD fix also closes Ch 2 :85 above (cited @BatesHastieTibshirani2024, already in the bib, instead of adding Bengio–Grandvalet); the Kleen-row fix resolves the measurement-error half of Ch 3 :108.

**Status 2026-08-07 (second pass): all 23 [S]/[T] lens items fixed as well; both chapters fully re-rendered with code re-execution.** Non-obvious calls: (i) the §2.8 tuning example now simulates a cross-section of 1,000 firm-year excess returns from ten standardized characteristics (interaction + quadratic term, noise sd 2) — the executed best config lands at CV MSE ≈ 4.8, just above the irreducible variance of 4, so the example behaves as designed; (ii) the `TimeSeriesSplit` sentence now credits `max_train_size` for sliding windows; (iii) the Bergmeir–Hyndman–Koo qualification was folded into the rewritten §2.7 opening sentence (new bib entry `BergmeirHyndmanKoo2018`), closing the second half of Ch 2 :84 — the pseudo-OOS naming half stays open under the bridges section; (iv) the Ch 3 synonym sweep went beyond the three flagged lines and normalized every non-definition "Log Score" (heading :470 kept its explicit `{#sec-mini-log-score-kl}` anchor, so the cross-file link from adv_tree_based_methods is unaffected); (v) the Ch 2 leakage-callout language "honest leave-one-out scaling" now has its definition anchor — LOO is defined in the Common Choices for K callout as the $K=n$ case.

**Status 2026-08-10 (methods re-check on the bridge session): five residuals fixed.** One escaped $\nabla_\theta L$ in Ex 5.2's solution converted; Ex 5.3 restructured to the observation-level $\ell_i$ with $\partial\ell_i/\partial z_i = p_i-y_i$ and the explicit average conclusion $\partial\mathcal{L}/\partial z_i=(p_i-y_i)/n$ (resolving the summed-NLL vs. average-loss conflict; the softmax collapse in the backprop section now carries its "up to $1/n$" clause); the AdaGrad–BHHH paragraph tightened ("gradients used across iterations — full-sample under the displayed notation, stochastic under mini-batching"; shared *construction* from gradient outer products with different statistical meanings; "adaptive cousins"); the regularization bullet renamed "$L_2$ Penalty (Ridge-Type Regularization)" with **weight decay** introduced as the related-but-distinct optimizer-level mechanism; the notation declaration scoped to "this chapter" until Ch 6/7 convert (Ch 6 still uses $L_T$ — widen the declaration when they do). Both chapters re-rendered.

**Status 2026-08-08 (bridge & notation session, Ch 4–5, per the methods-check review): all four items landed.** (i) Scoped loss convention: network chapters write $\mathcal{L}(\theta)$/$\ell$ with $L$ reserved for depth — ~20 displays converted in Ch 5 incl. the backprop recursion and the weight-decay display that previously used both meanings of $L$ in one formula; declaring sentence at the Loss Functions section; penalty renamed $P(\theta)$; Ex 5.3's likelihood made symbol-free. (ii) "local gate" → "activation derivative". (iii) Bridges: AdaGrad–BHHH as a delimited analogy (not an identity — common-$\theta$ scores/inverse vs. iteration gradients/inverse square root), defining preconditioning; NLS sentence at the MSE display; sieve paragraph with the UAT-is-not-consistency distinction; ridge-type citation with caveats. (iv) Four bib entries: `BerndtHallHallHausman1974`, `White1990`, `Chen2007`, `HoerlKennard1970`. Both chapters re-rendered; citations verified in the built HTML. Chapters 4–5 are now bridge-complete; "fully closed" awaits Ch 5's next external question-pass per the reviewer's caveat.

**Status 2026-08-07 (round-5 external audit, Ch 1–4): 36 of 39 findings applied.** The strongest external round — its new question/answer-coupling checks caught what older passes could not, including several regressions introduced by our own fixes. Highlights: the three-way $D_{\text{KL}}=0$ inconsistency across the Properties callout, Ex 1.3, and Common Pitfalls unified (same distribution; a.e. for densities); "reparameterization" replaced by "change of variables" for data transformations with an econometrician's note; two underdetermined reflection prompts (Ch 1 tail-shape, Ch 4 mini-batch dependence) rewritten so prompt and answer round-trip; $K$ removed from Ch 2's hyperparameter examples with a clarifying contrast; the LogS propriety-class finiteness condition stated (Cauchy-vs-Gaussian consistency with Ch 1); probability→density language corrected for continuous forecasts; Key Takeaway 3 aligned with the round-3 mechanism fix; Ex 3.4's exact-MA claim hedged and its answer-revealing annotation trimmed; Ex 2.3 and Ex 4.1 gained their missing conditions ($\beta\neq0,\sigma_\xi^2>0$; for-every-initial-value); Ex 4.2 P4 split into P4/P5 with the large-$N$/positive-autocovariance assumptions stated; the risk-spine bridges added (expected score = generalization risk in Ch 3's definition; empirical-loss-vs-risk and the Ch 2→Ch 4 notation map in Ch 4's Notation). Disputed: "This expression/identity" after displays (nouns present, A2-compliant); heading re-expansions resolved by a new B-T2 heading exemption in `principles.md` instead of text changes. Author decision recorded: lecture-note sources enter the bib only in author-supplied form — RMSprop now cites `hinton2012rmsprop` (Hinton's own slides, with URL) rather than a secondhand course attribution; B-C3 updated accordingly. All four chapters re-rendered.

**Status 2026-08-07 (student-lens pass, Ch 4 & Ch 5): both chapters audited and fixed in one round — 23 findings (2 blocking, 16 friction, 5 polish), all applied; every open July item for these chapters closed** (see their annotated entries). New catches beyond the July list: Ch 4 — "convex" used four subsections before its definition (now a Definition callout at first use), the momentum velocity renamed $u_t$ to end the $v_t$ velocity/second-moment overload (incl. Ex 4.3, recast in $\theta^{(t)}$), NLP/GPU/EMA expansions, "weight decay" glossed at AdamW, the 2D demo identified as Ex 4.1's quadratic, RMSprop's unpublished provenance noted, post-figure interpretation for the non-convex panel; Ch 5 — "vanishing gradients"/"RNNs" glossed with forward links, MSE/OLS expansion order, distributional-networks forward link, mermaid architecture diagram labeled `fig-ffn-architecture` and properly referenced, missing-blank-line and dangling-comma cosmetics, Roadmap item 5 made substantive. Remaining for these chapters: only the global clusters (loss-symbol unification incl. Ch 5's $L$ loss-vs-layer-count, "local gate" rename, bridges items Ch 4 AdaGrad=OPG/BHHH and Ch 5 NLS/sieve/ridge). Both chapters re-rendered; crossref checks clean.

**Status 2026-08-07 (round-3 external audit on the revised Ch 3): 11 of 12 findings applied.** Genuine catches: the Ex 3.4 exam annotation over-claimed the direction of the HAC correction — now conditioned on positive autocorrelation, with the $\theta=-1/2$ counterexample (ratio $0.2$) added to the solution; "avoid this leak" in Common Pitfalls reserved "leakage" language for information-set violations (same-sample residual shrinkage is an overfitting bias); expected-vs-average LogS distinction in §10 and Key Takeaway 5; full acronym sweep (GDP, FRED-QD, AR(1), GARCH, OOB, RF, ML, HAC, i.i.d. expanded; PIT de-duplicated to the Roadmap); distribution/density-forecast synonym declared at the definition; the skew-PIT illustration now uses `skewnorm.stats` population moments instead of estimating the forecast from the evaluated outcomes; four teaching-figure captions made self-contained; within-score-only comparability note on the summary bars; light hedging of the winner language ("in this sample", "same-sample overfitting bias"); Ex 3.3 P2 hint dropped; Ex 3.2 P3 marked as the complementary derivation route — the audit's "repeats a worked derivation" claim was disputed (integral definition vs kernel representation). Reliability note: this audit hallucinated figure labels and one caption quote; all quotes were verified against the file before applying. Chapter re-rendered.

**Status 2026-08-07 (round-2 external audit on the revised Ch 2): 9 further fixes applied.** A re-run of the external lens on the revised chapter surfaced three genuine new findings, all fixed: cross-sectional data are no longer equated with exchangeability (§6 scope sentence + §8 justification now say *independently sampled* cross-section without cluster/spatial dependence); the Econometric Warning no longer claims that boundary dependence per se makes the estimate optimistic — optimism is now reserved for training-after-validation designs and overlapping labels, with dependence recast as a deployment-horizon-matching issue; Ex 2.1 gained the $T\ge 3$, $s_{-j}^2>0$ nondegeneracy assumption. Residuals also closed: pre-gloss "architecture" (§4, §5 → "model specification(s)"), Roadmap item 4 window gloss, i.i.d. moved before the display, AR(1) expansion order, "more robust" given its criterion. The two July exercise items (:86 nondegeneracy, :88 mechanical hints) were pulled forward and closed — see their entries above. Chapter re-rendered; checks clean.

### Ch 2 — cross_validation.qmd (lens pass)

- [x] **[E/B]** :10,:166-172,:405-562 — [B-S3] NN machinery three chapters early: Overview hyperparameter examples ("learning rate or regularization strength … layers and neurons"), the §5 tuning list (hidden layers, dropout, weight decay, activation functions), and the §8 example tuning an MLP (architecture, learning rate, L2). Replace Overview examples with objects the reader has (polynomial degree, penalty strength, number of folds); give §8 a black-box framing sentence + Ch 5 link, or move the example after Ch 5.
- [x] **[E/B]** :18 — [B-S4] Roadmap item 3 "explain how leakage inside folds invalidates the estimate" — leakage/folds/"the estimate" all undefined here. Restate in plain language; also gloss item 5's inner/outer/nested preview.
- [x] **[E/B]** :179 — [B-S1] "Inside each fold, every preprocessing or transformation rule …" — the leakage callout uses folds one section before K-fold defines them (:205). Move the callout after the K-fold definition.
- [x] **[E/B]** :40 — [B-X1] **false back-reference**: "As discussed in the previous chapter, under Gaussian errors … equivalent to minimizing the training sum of squared errors" — Ch 1 contains no least-squares discussion (verified by search; it shows MLE = min sample cross-entropy). Derive the one-liner here or route through Ch 1's actual statement.
- [x] **[E/B]** :12 — [B-S4] "nested validation as emphasized in @VarmaSimon2006 …" cold in the Overview (concept explained only at :551). Gloss inline or defer the term. *(Sentence dates from the July :83 fix — the fix introduced the leak.)*
- [x] **[E/A]** :544,:548 — [B-P2] "giving us both mean performance and uncertainty estimates" + the improvement-vs-fold-SD comparison. Extends the open item at Ch 2 :85 (fold SD is not a valid SE; Bengio–Grandvalet): strike "uncertainty estimates", call `std_cv_mse` descriptive fold heterogeneity, caveat the informal 1-SE-style screen, lean on the already-cited Bates–Hastie–Tibshirani.
- [x] **[S/B]** — [B-T1] abbreviation order/coverage (no whitelist): MSE first at :16, expanded :46; i.i.d. in math at :27, expanded :293; **MSPE** (:935) and **AR(1)** (:960) never expanded; tie $\widehat{\mathrm{CV}}_K$ to "cross-validation (CV)" once.
- [x] **[S/B]** :551 — [B-T3] "the outer test folds never enter the selection step" clashes with the chapter's reservation of *test set* for the one-time final split; say "outer evaluation folds".
- [x] **[S/B]** :290 — [B-N3] "observed and finalized in their current form by real-world time $t$" — ambiguous for provisional releases (in the set at $t$) vs later revisions (not). Define via then-current vintages. *(Refines the July :82 measurability fix.)*
- [x] **[S/A]** :203-211 — [B-P2] random K-fold presented before its validity condition (arrives only at :293); add a scope sentence + signpost to §7 at the top of §6. Related open item: Bergmeir–Hyndman–Koo qualification at Ch 2 :84.
- [x] **[S/A]** :309 — [B-P2] "Autocorrelation can also leak information" conflates train–validation dependence with the chapter's own measurability definition of leakage; separate the two mechanisms, motivate purging/embargo via overlapping labels and boundary-crossing feature construction.
- [x] **[S/B]** :588 — [B-S1] Ex 2.1 is about leave-one-out CV, never defined; add the "$K=T$ special case" clause in §6 or in the exercise statement.
- [x] **[S/C]** :433 (and the :71 $\sin(\pi x)$ toy) — `make_regression` vs "econometric data, always"; recast with an econometric DGP or a file-backed dataset from `datasets.qmd`.
- [x] **[S/B]** :293 — [B-P2] "it assumes that the data points are i.i.d." — procedures don't assume; the fold-mimics-unseen-data argument requires exchangeability, which random folds destroy under temporal dependence.
- [x] **[S/C]** :230-277,:334-401 — [B-R3] fig-kfold-cv and fig-timeseries-cv get no post-figure interpretation (stratification callout / API note follow immediately); one short paragraph each. Extends the open caption item at Ch 2 :89.
- [x] **[S/C]** :56,:338,:413 — generic captions not self-contained (panels/axes/colors/red star undefined). Extends Ch 2 :89.
- [x] **[S/B]** :403 — "A sliding window must be implemented manually" — false: `TimeSeriesSplit(max_train_size=…)` caps the training window; manual code only for nonstandard step patterns.
- [x] **[S/B]** :221,:320,:330,:770 — [B-A2] bare "This" after display/list/equation; name the noun ("This expected-risk target", "This retraining scheme", "The sliding window", "The strict inequality").
- [x] **[T/B]** :52 — [B-S1] "one-time test set" one section before test sets are defined; say "held-out sample".
- [x] **[T/B]** :281 — stratification "ensures that each fold has the same percentage" → "as closely as the integer counts allow".
- [x] **[T/B]** :141 — [B-T4] re-bolded **cross-validation** (definition-site bold already at :12).
- [x] **[T/C]** :553-562 — "For Larger Networks" tip: add a markdown link to `hpo.qmd`.
- [x] **[T/B]** :135 — "However, this simple approach has major drawbacks" contradicts the concession sentence before it; use "Specifically, using a single split this way has three drawbacks:".
- Already tracked above, confirmed unchanged by this pass: Ex 2.2 nondegeneracy (Ch 2 :86), recall-only reflection question (:87), mechanical hints (:88).

### Ch 3 — evaluating_distributions.qmd (lens pass)

- [x] **[E/B]** :96 — [B-S1] Evaluation Workflow callout: "LogS and CRPS address the first task. PIT histograms address the second." — all three undefined until §4–§8. Move the callout below the definitions or gloss each name.
- [x] **[E/B]** :20 — [B-T1] Roadmap "**LogS** and **CRPS**" naked; expand there ("logarithmic score (LogS)", "continuous ranked probability score (CRPS)") and drop the then-duplicate expansion at :140.
- [x] **[E/B]** :22 — [B-T1] "KL divergence" never expanded in this chapter ("Kullback–Leibler" absent, verified); expand at the Roadmap first use.
- [x] **[E/B]** :370 — [B-N2] "$u_t = F_t(y_{t+1})$ should be i.i.d." — mixed clocks: origin-indexed sequence, target-dated argument. State the $F_{t+1\mid t}$ convention once and index PITs by target date throughout §8. *(Refines the closed July :101 item, which moved the argument to $y_{t+1}$ but left $u_t$ on the origin clock.)*
- [x] **[E/B]** :466,:471 — [B-X2] §9–§10 use $\mathbb{H}_{ce}(p,q)$, $\mathbb{H}(p)$ for *densities*, but Ch 1 reserves $\mathbb{H}$ for the discrete case and $h$, $h_{ce}$ for the continuous one — "Recall the fundamental relationship" recalls it in the wrong notation and drops Ch 1's differential-entropy-can-be-negative caveat. Switch to $h_{ce}$, $h$ plus one clause.
- [x] **[E/B]** :343 — [B-C2] "Measurement Error Sensitivity | High | Low" — resolves the open "explain or cut" at Ch 3 :108: the row is @Kleen2024 (in bib, cited only in Ch 9). Add the citation + one sentence saying what is measured with error and why LogS rankings suffer more.
- [x] **[S/B]** :149-153,:279-285 — [B-A1/A3] "It"-bullets under unnamed "**Properties**:"/"**Intuition**:" labels, the CRPS set sitting directly after fig-crps-viz; name the object in the label and open the first bullet with the noun. (MAE re-expansion at :281 and CDF re-expansion at :368 are the [B-T2] duplicates — first uses :32 and :221.)
- [x] **[S/B]** :587 (with :570-572) — [B-N4] code `y_t = …["y"]` where `y` is `shift(-1)`: the variable named `y_t` holds $y_{t+1}$. Rename `y_next` + one prose sentence on the row convention.
- [x] **[S/B]** :524,:537 — [B-S2] "the same FRED-QD macro panel that appears in the tree chapters" / "the same test window used in the tree chapters" — forward anchor on Ch 10–13; link `datasets.qmd` (extends Ch 3 :107) and say "revisited in later chapters". *(The random-forest gloss at :528 is the model for handling forward machinery — keep as is.)*
- [x] **[S/C]** :51,:162,:236,:382 — four figure chunks still `echo: false` against the foldable-code convention; this chapter was missed by the global :21 fix.
- [x] **[T/B]** :377 — typo "a mismatch in skewnessthough" → "skewness — though". *(Likely introduced by the July :97 skew-description rewrite.)*
- [x] **[T/B]** :144,:155,:459 — [B-T3] LogS / Log Score / Logarithmic Score drift; synonyms once at the definition site, then one form.
- Already tracked above, confirmed by this pass: $F$ forecast/true flip incl. Ex 3.2 (Ch 3 :104 + global :35), interpretation-heavy captions (:108), "Beyond LogS and CRPS" one-callout section and Roadmap omissions (:107).
